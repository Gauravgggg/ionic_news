import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.task',
  appName: 'news-ionic-pro',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
